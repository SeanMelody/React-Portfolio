import React from 'react'

const HeaderStyles = (props) => {
    return (
        <header>
            <h1>
                {props.title}
            </h1>
            <br></br>
        </header>
    )
}

export default HeaderStyles
